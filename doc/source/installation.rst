============
Installation
============

At the command line::

    $ pip install commissaire_openstack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv commissaire_openstack
    $ pip install commissaire_openstack
