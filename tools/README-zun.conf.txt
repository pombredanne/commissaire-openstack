To generate the sample commissaire_openstack.conf file, run gen-config tools
from the top level of the commissaire_openstack directory:

tools/gen-config

Or run the following command from the top level of the
commissaire_openstack directory:

tox -egenconfig

Generated sample configuration file will be put into etc/commissaire_openstack/
directory.
